<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.AssetMapper">
  <resultMap id="BaseResultMap" type="com.example.demo.entity.Asset">

    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="num" jdbcType="VARCHAR" property="num" />
    <result column="rfid" jdbcType="VARCHAR" property="rfid" />
    <result column="state" jdbcType="INTEGER" property="state" />
  </resultMap>
  <sql id="Base_Column_List">
    id, name, num, rfid, state
  </sql>
  
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from asset
    where id = #{id,jdbcType=INTEGER}
  </select>
  
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from asset
    where id = #{id,jdbcType=INTEGER}
  </delete>
  
  <insert id="insert" parameterType="com.example.demo.entity.Asset">
    insert into asset (name, num, rfid)
    values (#{name,jdbcType=VARCHAR}, #{num,jdbcType=VARCHAR}, 
      #{rfid,jdbcType=VARCHAR})
  </insert>
  
  <insert id="insertSelective" parameterType="com.example.demo.entity.Asset">
    insert into asset
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="name != null">
        name,
      </if>
      <if test="num != null">
        num,
      </if>
      <if test="rfid != null">
        rfid,
      </if>
      <if test="state != null">
        state,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="num != null">
        #{num,jdbcType=VARCHAR},
      </if>
      <if test="rfid != null">
        #{rfid,jdbcType=VARCHAR},
      </if>
      <if test="state != null">
        #{state,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  
  <update id="updateByPrimaryKeySelective" parameterType="com.example.demo.entity.Asset">
    update asset
    <set>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="num != null">
        num = #{num,jdbcType=VARCHAR},
      </if>
      <if test="rfid != null">
        rfid = #{rfid,jdbcType=VARCHAR},
      </if>
      <if test="state != null">
        state = #{state,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  
  <update id="updateByPrimaryKey" parameterType="com.example.demo.entity.Asset">
    update asset
    set name = #{name,jdbcType=VARCHAR},
      num = #{num,jdbcType=VARCHAR},
      rfid = #{rfid,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
  
  <select id="find"  resultType="Asset">
    SELECT
    <include refid="Base_Column_List" />
    FROM asset
  </select>

  <select id="getasset" resultType="Asset">
     SELECT
     <include refid="Base_Column_List" />
     FROM asset WHERE id = #{id,jdbcType=INTEGER}
  </select>
  
  <select id="getAssetId" resultType="Asset">
     SELECT
     <include refid="Base_Column_List" />
     FROM asset WHERE id = #{id,jdbcType=INTEGER}
  </select>
  
  <select id="getAllAsset" resultType="Asset">
  	 SELECT
     <include refid="Base_Column_List" />
     FROM asset
  </select>
  
   <select id="getAssetName" resultType="Asset">
  	 SELECT
     <include refid="Base_Column_List" />
     FROM asset WHERE name = #{name,jdbcType=VARCHAR}
  </select>
  
  <select id="findDepts" parameterType="java.util.Map" resultType="java.util.LinkedHashMap">
	  SELECT
     <include refid="Base_Column_List" />
     FROM asset 
     <where>
     	<if test="name != null">name like "%"#{name}"%"</if>
     	<if test="num != null">and num = #{num}</if>
     </where> 
  </select>
  
  <select id="getAssetList" resultType="Asset">
  	 SELECT
     <include refid="Base_Column_List" />
     FROM asset WHERE CONCAT(name,num,rfid) LIKE CONCAT('%',#{name},'%')
  </select>
  <insert id="add" parameterType="Asset">
    insert into asset (name, num, rfid)
    values (#{name,jdbcType=VARCHAR}, #{num,jdbcType=VARCHAR}, 
      #{rfid,jdbcType=VARCHAR})
  </insert>  
</mapper>